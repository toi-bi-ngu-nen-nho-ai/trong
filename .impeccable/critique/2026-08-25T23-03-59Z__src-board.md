---
target: MindMapScreen
total_score: 29
max_score: 32
na_heuristics: 7,10
p0_count: 0
p1_count: 0
timestamp: 2026-08-25T23-03-59Z
slug: src-board
---
Method: dual-agent (A: design-review sub-agent · B: detector+browser sub-agent)

# Critique: MindMapScreen (src/board/) — lượt 3, sau khi vá 4 phát hiện lượt 2

## Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 4/4 | Ink-bloom loading, ảnh xem trước phủ canvas tới `onReady`, cửa sổ xác nhận xoá 5s |
| 2 | Match System / Real World | 3/4 | Texture giấy + doodle + huy hiệu đọc như "sổ tay thật"; ẩn dụ vật liệu vẫn dừng ở trang trí nền |
| 3 | User Control and Freedom | 4/4 | Xoá mềm 2 lớp phục hồi, Escape huỷ đổi tên, "quay lại" luôn có |
| 4 | Consistency and Standards | 4/4 | Đo trực tiếp: `--c-accent-2` dark-mode giống hệt nhau ở CẢ 2 nút "Hoàn tác" (toast + panel) |
| 5 | Error Prevention | 4/4 | Xác nhận xoá 2 bước, tên rỗng fallback, tag trùng bị chặn |
| 6 | Recognition Rather Than Recall | 3/4 | Chip/ô tìm rõ tên, nhưng không gợi ý ô tìm+chip lọc kết hợp kiểu AND |
| 7 | Flexibility and Efficiency | n/a | Hợp lệ ở mode Experience theo brief |
| 8 | Aesthetic and Minimalist Design | 3/4 | Nghiêng về minimalist hơn "đẩy hiệu ứng tới giới hạn" mà hiến chương yêu cầu |
| 9 | Error Recovery | 4/4 | Màn lỗi "Cần mạng để tải lần đầu" rõ, nút "Thử lại" dựng lazy() mới thật |
| 10 | Help and Documentation | n/a | Hợp lý cho canvas sáng tạo |
| **Total** | | **29/32** | **Excellent** (90.6%) |

*Trend thang điểm: lượt 1 = 28/36 (H10 n/a), lượt 2 = 32/40 (đủ 10 mục), lượt 3 = 29/32 (H7+H10 n/a) — ba lượt dùng ba tập heuristic áp dụng khác nhau tuỳ đánh giá của từng agent, xem % để so sánh tương đối.*

## Design Specificity Verdict

**LLM (A)**: Bề mặt đã rời "generic gallery" — mọi quyết định thị giác (chấm màu, biến thể doodle, góc nghiêng, hue chip) bind vào `bang.id` bằng hash ổn định. Nhưng vẫn ở mức "tinh chỉnh đúng" chứ chưa "đẩy hết cỡ" như hiến chương đòi — 3 lượt qua đều dùng ngân sách vá lỗi (hợp lý, cần thiết) chứ chưa bồi thêm hiệu ứng vật liệu mới.

**Deterministic scan**: `detect.mjs` vẫn sạch (`[]`). Overlay tìm thấy `clipped-overflow-container` x3 trên `main.relative...has-nav` — nghi ngờ đây là layout cấp App shell, ngoài phạm vi `src/board` (chưa xác nhận độc lập bởi A). `dark-glow`/`undersized-ui-text` vẫn là các phát hiện đã xác minh ngoài phạm vi/chủ đích từ 2 lượt trước (toolbar nổi hợp lệ, bottom-nav dùng chung toàn app).

**Visual overlays**: Injection thành công ở 4 trạng thái, console `impeccable` đọc trực tiếp. B ghi nhận một chi tiết kỹ thuật: ancestor `.card-settle` có `transform` đang chạy lúc đo khiến `position: fixed` của menu bị containment theo ancestor thay vì viewport theo đúng ngữ nghĩa CSS chuẩn — nhưng đây gần như chắc chắn là **artifact do B click quá nhanh** (bắt được thẻ giữa lúc animation entrance 0.26s còn chạy, việc không thể xảy ra với thao tác người thật). Assessment A độc lập đo lại chính phần tử này ở trạng thái nghỉ và xác nhận `position: fixed; bottom: 66px` (đúng vùng ngón cái) — không phát hiện vấn đề containing-block nào. Kết luận: không phải lỗi thật.

## Overall Impression

Cả 3 mức P1 từ lượt trước (contrast, continuity, cognitive load) giờ được xác nhận vững qua đo đạc trực tiếp CỦA MỘT AGENT ĐỘC LẬP không biết gì về các lượt vá trước — 11/11 chuyên khoa đạt AA ở cả hai theme (dark 4.88–6.47:1), 2 màu "Hoàn tác" đã hợp nhất, doodle 3 bảng đo được thật sự khác nhau (path count, transform, hue). **Không còn P0/P1 nào trong lượt này.** Vấn đề còn lại chuyển hẳn từ "lỗi cần vá" sang "khoảng cách chiến lược cần quyết định" (P2) và một khe hở nhỏ trong reduced-motion gate (P3).

## What's Working

1. **Contrast fix giữ vững qua toàn bộ 11 chuyên khoa, cả hai theme** — đo trực tiếp bởi agent độc lập, không chỉ đọc code.
2. **Doodle biến thể + continuity FLIP + mobile bottom-sheet đều là cơ chế thật**, đo được số liệu cụ thể khớp thiết kế, không phải chỉ "nằm trên giấy".
3. **Hai nút "Hoàn tác" đã thống nhất màu** — xác nhận bằng đo trực tiếp cả hai vị trí trong cùng một phiên.

## Priority Issues

**[P2] Khoảng cách hiến chương ("đẩy hiệu ứng tới giới hạn", "≥50% công sức") vẫn còn ở tầng gallery — không phải lỗi, là thiếu tham vọng**
- Why it matters: 3 lượt critique liên tiếp đều dùng ngân sách vá lỗi (đúng và cần thiết) nhưng phần "đẩy tới giới hạn rồi để chủ dự án cắt" — bắt buộc theo hiến chương — chưa có bước tiến nào. Cụ thể còn thiếu ở tầng wrapper (không đụng vendor): không có phản hồi "vật liệu" khi kéo/thả sắp xếp thẻ; panel sửa tag dùng `.mind-sheet` chung với cả app, không có gì phân biệt "giấy ghim vào Mindmap".
- Fix: Một phiên làm việc riêng, chủ đề rõ "đẩy vật liệu ở gallery" — không phải vòng critique→vá lặp lại nữa.
- Suggested command: `/impeccable overdrive` (hoặc `/impeccable delight`)

**[P2] Trạng thái rỗng lần đầu (persona Jordan) vẫn dừng ở copy, chưa ở cảm xúc**
- Why it matters: Dòng gợi ý giá trị (đã thêm ở lượt 2) là cải thiện thật, nhưng vẫn là VĂN BẢN giải thích, không phải một khoảnh khắc thị giác CHỨNG MINH giá trị đó. Với tuyên bố "≥50% công sức thiết kế toàn app", ấn tượng đầu (doodle mờ 40% + đoạn văn + nút "+") chưa tương xứng.
- Fix: Cân nhắc một minh hoạ động/mẫu xem trước thay vì chỉ text.
- Suggested command: `/impeccable onboard`

**[P3] `.board-flip-cover` bị bỏ sót khỏi danh sách gate `prefers-reduced-motion`**
- Why it matters: `src/index.css` khối reduced-motion liệt kê `.board-flip-start`/`.board-flip-run` nhưng thiếu `.board-flip-cover` (lớp phủ ảnh xem trước, `transition: opacity 0.25s`) — người bật "Giảm chuyển động" vẫn nhận đúng 0.25s crossfade thay vì tức thời, dù đây là cùng cơ chế continuity đã được gate.
- Fix: Thêm `.board-flip-cover` vào danh sách selector của khối `@media (prefers-reduced-motion: reduce)`.
- Suggested command: `/impeccable harden`

**[P3] Dải chip mở rộng nhảy thẳng từ 4 lên 13 lựa chọn, không phân nhóm**
- Why it matters: Bấm "Thêm +9" hiện toàn bộ 9 chuyên khoa còn lại cùng lúc trong một hàng cuộn ngang phẳng. Opt-in nên không cấp bách, nhưng quét/cuộn qua 9 mục để tìm một khoa cụ thể không lý tưởng.
- Fix: Nhóm theo hệ cơ quan hoặc thêm ô lọc-trong-lọc nếu số chuyên khoa còn tăng.
- Suggested command: `/impeccable distill`

## Persona Red Flags

**Alex (trực gấp, cần tra lại một board cụ thể)**: Không có red flag mới — ô tìm + FLIP mở board (~0.4-0.6s) đủ nhanh.

**Jordan (lần đầu mở tab)**: Xem P2 ở trên — copy đã tốt hơn nhưng chưa đủ "wow" để chứng minh tuyên bố ≥50% công sức thiết kế.

**Casey (mạng bệnh viện chậm)**: `.mind-loading-ink` đúng hướng, xác nhận qua code — không tái hiện được mạng chậm thật trong dev để đo trực tiếp thời gian phản hồi.

## Minor Observations

- Format thời gian tương đối ("42 phút trước") đọc rõ, hợp lý cho ngữ cảnh trực.
- `aria-label` đo trực tiếp đúng: `"Mở bảng {tên}, chuyên khoa {tên khoa}"` — xác nhận fix a11y lượt 2 hoạt động.
- Double-click nhanh vào "Thêm ▾" toggle đúng, không kẹt state.
- `clipped-overflow-container` x3 từ detector nhắm vào `main...has-nav` — nghi ngờ thuộc App shell, ngoài phạm vi `src/board`; cần xác nhận riêng nếu muốn xử lý.
- Icon tab "Mindmap" trong `App.tsx` dùng path SVG rất lớn (>10KB text) nhúng trực tiếp — không phải vấn đề thiết kế, đáng để mắt về bundle size nếu có dịp.

## Questions to Consider

- Ranh giới "gallery" (sửa được) vs "canvas" (vendor, không đụng) đang khiến cải tiến chỉ chạm lớp vỏ — có nên định nghĩa lại phạm vi "≥50% công sức thiết kế" để bao gồm những gì làm được ở wrapper (kéo-thả, sheet có chất liệu riêng)?
- "Đã xoá gần đây" hiện là lưới an toàn tối thiểu — có cần một màn "Thùng rác" đầy đủ, hay mức hiện tại đã đủ?
- Có nên tách hẳn một phiên "đẩy hiệu ứng tới giới hạn" riêng, không phải critique, đúng tinh thần hiến chương 2026-08-10?
